import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'DekDee Thailand | Find the Best Schools in Thailand',
  description: 'Discover, compare, and connect with the best schools across Thailand. International, private, Islamic, and budget-friendly schools. Your one-stop solution for education in Thailand.',
  keywords: ['schools in Thailand', 'international schools Bangkok', 'private schools Thailand', 'education Thailand', 'best schools Bangkok'],
  generator: 'DekDee Thailand',
  icons: {
    icon: '/logo.png',
    apple: '/logo.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
